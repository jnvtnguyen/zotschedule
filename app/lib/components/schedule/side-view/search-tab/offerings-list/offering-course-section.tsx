import { WebSocSection } from "@/lib/uci/offerings/types";

type SideViewOfferingCourseSectionProps = {
  section: WebSocSection;
};

export function SideViewOfferingCourseSection({
  section,
}: SideViewOfferingCourseSectionProps) {
  return <></>;
}
